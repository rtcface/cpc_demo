---
import type { InformationEntry } from "@/types";
import EntryLayout from "@components/common/information/EntryLayout.astro";
import { getCollection } from "astro:content";

const entries = await getCollection("informacion");
const entry = entries.find(entry => entry.id === "semblanzas") as InformationEntry;
---

<EntryLayout entry={entry} /> 